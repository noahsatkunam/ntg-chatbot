import { Router } from 'express';

const router = Router();

// Placeholder for tenant routes
router.get('/', (_req, res) => {
  res.json({ message: 'Tenant routes not implemented yet' });
});

export default router;
